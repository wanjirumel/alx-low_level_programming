#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the value of an int
 * @r: - int to be checked
 * Return: - abs of int
 */
int _abs(int r)
{
	if (r >= 0)
	return (r);
	else
	return (-r);
}
