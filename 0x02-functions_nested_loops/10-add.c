#include "main.h"
#include <stdio.h>

/**
 * add -  adds two integers and returns the result
 * @x: - first num to be checked
 * @y: - second num to be checked
 * Return: Integer value
 */
int add(int x, int y)
{
	return (x + y);
}
